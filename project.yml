name: SmartCard
options:
  bundleIdPrefix: com.smartcard
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "16.0"

settings:
  base:
    SWIFT_VERSION: "5.9"
    DEVELOPMENT_TEAM: ""

packages:
  Firebase:
    url: https://github.com/firebase/firebase-ios-sdk
    from: "11.0.0"
  PlaidLink:
    url: https://github.com/plaid/plaid-link-ios
    from: "5.6.0"

targets:
  SmartCard:
    type: application
    platform: iOS
    sources:
      - path: SmartCard
        excludes:
          - "**/.DS_Store"
    settings:
      base:
        INFOPLIST_GENERATION_MODE: GeneratedFile
        PRODUCT_BUNDLE_IDENTIFIER: com.smartcard.app
        TARGETED_DEVICE_FAMILY: "1"
        MARKETING_VERSION: "1.0.0"
        CURRENT_PROJECT_VERSION: "1"
        GENERATE_INFOPLIST_FILE: YES
        INFOPLIST_KEY_UIApplicationSceneManifest_Generation: YES
        INFOPLIST_KEY_UIApplicationSupportsIndirectInputEvents: YES
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
        INFOPLIST_KEY_UISupportedInterfaceOrientations_iPhone: "UIInterfaceOrientationPortrait"
        INFOPLIST_KEY_NSCameraUsageDescription: "SmartCard uses the camera to scan receipts for quick transaction entry."
        INFOPLIST_KEY_NSPhotoLibraryUsageDescription: "SmartCard accesses your photo library to import receipt images."
        INFOPLIST_KEY_CFBundleDisplayName: "SmartCard"
        INFOPLIST_KEY_LSApplicationCategoryType: "public.app-category.finance"
    resources:
      - path: SmartCard/Resources/Assets.xcassets
      - path: SmartCard/Resources/Preview Content
      - path: SmartCard/GoogleService-Info.plist
    dependencies:
      - package: Firebase
        product: FirebaseAuth
      - package: Firebase
        product: FirebaseFirestore
      - package: PlaidLink
        product: LinkKit
      - target: SmartCardWidget
        embed: true
    entitlements:
      path: SmartCard/SmartCard.entitlements
      properties:
        com.apple.security.application-groups:
          - group.com.smartcard.app
    scheme:
      testTargets:
        - SmartCardTests
      storeKitConfiguration: SmartCard.storekit

  SmartCardWidget:
    type: app-extension
    platform: iOS
    sources:
      - path: SmartCardWidget
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.smartcard.app.widget
        TARGETED_DEVICE_FAMILY: "1"
        MARKETING_VERSION: "1.0.0"
        CURRENT_PROJECT_VERSION: "1"
        GENERATE_INFOPLIST_FILE: NO
        INFOPLIST_FILE: SmartCardWidget/Info.plist
        APPLICATION_EXTENSION_API_ONLY: YES
    entitlements:
      path: SmartCardWidget/SmartCardWidget.entitlements
      properties:
        com.apple.security.application-groups:
          - group.com.smartcard.app

  SmartCardTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - path: SmartCardTests
    dependencies:
      - target: SmartCard
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.smartcard.app.tests
        GENERATE_INFOPLIST_FILE: YES
